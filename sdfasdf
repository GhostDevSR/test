root@vps-a07248be:/var/www/Shieldflare/frontend# sed -n '180,260p' /var/www/Shieldflare/frontend/src/pages/dashboard/Analytics.jsx
          </div>
        )}

        {/* Domain analytics */}
        {mode==='domain' && selected && (
          <>
            {/* Stats */}
            <div style={{ display:'grid', gridTemplateColumns:'repeat(4,1fr)', gap:14, marginBottom:20 }}>
              <StatCard label="Total requests"  value={loading?'—':totalReq.toLocaleString()}              color="var(--blue)"   bg="var(--blue-bg)"   border="var(--blue-b)"   icon="📊" loading={loading}/>
              <StatCard label="Blocked"        value={loading?'—':(data?.totalBlocked||0).toLocaleString()} color="var(--red)"    bg="var(--red-bg)"    border="var(--red-b)"    icon="🛡️" loading={loading}/>
              <StatCard label="Allowed"        value={loading?'—':(data?.totalAllowed||0).toLocaleString()} color="var(--green)"  bg="var(--green-bg)"  border="var(--green-b)"  icon="✅" loading={loading}/>
              <StatCard label="Bandwidth"        value={loading?'—':fmtBw(totalBw)}                         color="var(--purple)" bg="#f5f3ff"           border="#ddd6fe"         icon="📡" loading={loading}/>
            </div>

            {/* Traffic chart */}
            {!loading && chartData.length>0 && (
              <div style={{ background:'var(--surface)', border:'1px solid var(--border)', borderRadius:'var(--r-lg)', padding:'20px', boxShadow:'var(--shadow-sm)', marginBottom:16 }}>
                <div style={{ display:'flex', justifyContent:'space-between', alignItems:'center', marginBottom:16 }}>
                  <div style={{ fontSize:14, fontWeight:600 }}>Traffic overview</div>
                  <div style={{ display:'flex', gap:14, fontSize:12 }}>
                    {[['#22c55e','Normal'],['#ef4444','Blocked']].map(([c,l])=>(
                      <div key={l} style={{ display:'flex', alignItems:'center', gap:5 }}>
                        <div style={{ width:10, height:10, borderRadius:'50%', background:c }}/>
                        <span style={{ color:'var(--text-dim)' }}>{l}</span>
                      </div>
                    ))}
                  </div>
                </div>
                <ResponsiveContainer width="100%" height={220}>
                  <AreaChart data={chartData} margin={{ top:5, right:5, bottom:5, left:0 }}>
                    <defs>
                      <linearGradient id="gNormal"  x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#22c55e" stopOpacity={.15}/><stop offset="95%" stopColor="#22c55e" stopOpacity={0}/></linearGradient>
                      <linearGradient id="gBlocked" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#ef4444" stopOpacity={.15}/><stop offset="95%" stopColor="#ef4444" stopOpacity={0}/></linearGradient>
                    </defs>
                    <XAxis dataKey="label" tick={{ fontSize:11, fill:'#94a3b8' }} axisLine={false} tickLine={false}/>
                    <YAxis tick={{ fontSize:11, fill:'#94a3b8' }} axisLine={false} tickLine={false}/>
                    <Tooltip content={<CustomTooltip/>}/>
                    <Area type="monotone" dataKey="normal"  stroke="#22c55e" strokeWidth={2} fill="url(#gNormal)"  name="Normal"/>
                    <Area type="monotone" dataKey="blocked" stroke="#ef4444" strokeWidth={2} fill="url(#gBlocked)" name="Blocked"/>
                  </AreaChart>
                </ResponsiveContainer>
              </div>
            )}

            {/* Top attackers + countries */}
            {!loading && (data?.topAttackers?.length>0 || data?.topCountries?.length>0) && (
              <div style={{ display:'grid', gridTemplateColumns:'1fr 1fr', gap:16, marginBottom:16 }}>
                {/* Top attackers */}
                {data.topAttackers?.length>0 && (
                  <div style={{ background:'var(--surface)', border:'1px solid var(--border)', borderRadius:'var(--r-lg)', overflow:'hidden', boxShadow:'var(--shadow-sm)' }}>
                    <div style={{ padding:'14px 20px', borderBottom:'1px solid var(--border)', fontSize:14, fontWeight:600 }}>🔥 Top attackers</div>
                    {data.topAttackers.slice(0,8).map((a,i) => (
                      <div key={a.ip} style={{ display:'flex', alignItems:'center', gap:10, padding:'10px 20px', borderBottom:i<data.topAttackers.length-1?'1px solid var(--border)':'none' }}>
                        <span style={{ fontSize:12, color:'var(--text-dim)', width:20, fontWeight:600 }}>#{i+1}</span>
                        <span style={{ fontFamily:'var(--mono)', fontSize:12, fontWeight:600, flex:1 }}>{a.ip}</span>
                        <span style={{ fontSize:11, color:'var(--text-dim)' }}>{a.country}</span>
                        <span style={{ fontSize:12, color:'var(--red)', fontWeight:700 }}>{a.count}×</span>
                      </div>
                    ))}
                  </div>
                )}

                {/* Top countries */}
                {data?.topCountries?.length>0 && (
                  <div style={{ background:'var(--surface)', border:'1px solid var(--border)', borderRadius:'var(--r-lg)', padding:'20px', boxShadow:'var(--shadow-sm)' }}>
                    <div style={{ fontSize:14, fontWeight:600, marginBottom:14 }}>🌍 Attacks per country</div>
                    <ResponsiveContainer width="100%" height={180}>
                      <PieChart>
                        <Pie data={data.topCountries} dataKey="count" nameKey="country" cx="50%" cy="50%" outerRadius={70} label={({country,percent})=>`${country} ${(percent*100).toFixed(0)}%`} labelLine={false} fontSize={11}>
                          {data.topCountries.map((_,i)=><Cell key={i} fill={COLORS[i%COLORS.length]}/>)}
                        </Pie>
                        <Tooltip formatter={(v,n)=>[v, n]}/>
                      </PieChart>
                    </ResponsiveContainer>
                  </div>
                )}
              </div>
            )}

            {/* GeoIP map */}
            {!loading && (data?.topCountries?.length>0 || ipData?.topCountries?.length>0) && (
root@vps-a07248be:/var/www/Shieldflare/frontend#
